def increment(number, by=1): #If we don't pass the second value then the default value will be used and the default parameter should be defined after the required parameters.
    return number + by


print(increment(2))
